[build-system]
requires = ["setuptools>=68", "cython>=3.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fairino"
version = "2.2.3"
description = "Fairino robot Python SDK (Cython-compiled)"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["fairino*"]
